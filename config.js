const ideoApiKey = 'FRjh@fdf84J83hfE'

const diceAnimationInterval = 0.25 * 1000

const minimalDiceAnimationDuration = 2 * 1000

const finishAnimationDelay = 0.01 * 1000

export {
    ideoApiKey,
    diceAnimationInterval,
    minimalDiceAnimationDuration,
    finishAnimationDelay
}